services:
  db:
    image: postgres:18
    environment:
      POSTGRES_DB: user
      POSTGRES_USER: password
      POSTGRES_PASSWORD: relay
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data:
