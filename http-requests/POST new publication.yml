info:
  name: POST new publication
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8000/publications
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
